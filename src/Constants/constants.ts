const BASE_URL_PREFIX = "http://";
const URL = "localhost:"
const BASE_URL_PORT = "4000";

const BASE_URL = BASE_URL_PREFIX + URL + BASE_URL_PORT;

const GET_RESERVATIONS = '/getReservations';

export {BASE_URL, GET_RESERVATIONS};
